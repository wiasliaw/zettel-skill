---
type: permanent
created: "{{date}}T{{time}}"
updated: "{{date}}T{{time}}"
tags: [zettelkasten, "{{top}}"]
version: "{{version}}"
sources:
  - "[[{{source-note}}]]"
intent: "{{intent}}"
review: "{{review}}"
---
## {{title}}

<!-- Frontmatter provenance fields: `sources` is a wikilink list pointing from
this card to the notes it was extracted from (direction is always card ->
source; append another entry when the same claim recurs in a new source);
`intent` names the active intent followed when this card was verified (empty
if none); `review` must carry the review conclusion: for cross-source
synthesis cards, a one-line adversarial-review conclusion note
(faithful/doubtful counts and the doubtful items); for single-document
extraction cards, no re-review — copy the source doc's frontmatter `review`
note and mark the inheritance (value format e.g. `承自 [[source]] 歸檔審查：<note>`).
Body: open with a bold one-line claim = this note's core point; then split
into ## sections as the content warrants (headings named by content, count
varies). Inline [[link]] another atomic note where the prose names it; no
"related" list, reverse direction via backlinks. This comment is guidance for
the writer and must be removed from the finished note. -->
